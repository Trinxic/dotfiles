-- [[ Bufferline ]]
-- displays buffers (kind of like tabs)

return {
  -- 'akinsho/bufferline.nvim',
  -- version = "*",
  -- dependencies = 'nvim-tree/nvim-web-devicons',
  notif = function()
    print 'bufferline is not configured...'
  end
}
